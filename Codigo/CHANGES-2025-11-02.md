Cambios realizados (02/11/2025)

- Reseñas en hoteles (qué y dónde): se habilitó crear y ver reseñas desde la API y se muestran en la página de detalle del hotel; navegación desde Home a `/hotels/:id` con botón “Ver detalles”.
- Modal de reserva: “Ver Disponibilidad” abre el modal sin preseleccionar habitación; la elección se hace dentro del modal (primer paso).
- Menú y breadcrumb: se añadió barra de navegación (Hoteles, Información, Sobre, Contáctanos) y breadcrumb visible en Home y Detalle.
- Contáctanos al pie: la sección de contacto pasó al footer con `mailto:` y mejor tipografía/espaciado general.
- Paleta de colores unificada: variables CSS aplicadas a botones, encabezados, cards y modal para una gama consistente.
- Slider/galería estable: imágenes centradas con `object-fit: cover` y sin desplazamientos; thumbnails defectuosas se ocultan y hay fallback de imagen.
- Limpieza de accesibilidad: se eliminó “Saltar al contenido principal” del HTML y se ocultó por CSS como respaldo.
- Cache de frontend (dev): HTML servido con `no-store`; archivos estáticos con expiración corta para ver cambios tras rebuild.
- Arreglo de build: se corrigieron duplicados de import y cierres JSX; el build finaliza correctamente.

Evidencia (figuras)
- Figura 1: Reseñas en detalle (POST y listado actualizado).
- Figura 2: Navegación Home → Detalle con “Ver detalles”.
- Figura 3: Modal de reserva abierto (paso 1, sin preselección).
- Figura 4: Menú principal y breadcrumb visibles.
- Figura 5: Footer con “Contáctanos”.
- Figura 6: Slider sin recortes (object-fit: cover).
- Figura 7: UI con paleta unificada.
- Figura 8: Encabezados Cache-Control (HTML no-store).

